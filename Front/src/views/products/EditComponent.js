/*import axios from 'axios';

// Fonction pour mettre à jour un composant
const updateComposent = async (id, composentData) => {
    try {
        const response = await axios.put(`https://votre-api.com/api/composents/${id}`, composentData);
        console.log('Composant mis à jour avec succès:', response.data);
    } catch (error) {
        console.error('Erreur lors de la mise à jour du composant:', error);
    }
};

// Exemple d'utilisation de la fonction
const handleUpdate = () => {
    const composentId = 1; // L'ID du composant que vous souhaitez mettre à jour
    const updatedComposent = {
        id: composentId,
        nom: 'Cable',
        sn: '12345',
        etatComposent: 'Besoin maintenance',
        etatCommande: 'Urgent',
        productId: 2
    };

    updateComposent(composentId, updatedComposent);
};
*/